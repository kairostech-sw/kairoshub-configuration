- platform: template
  sensors:
    heater_auto:
      friendly_name: "Programma caldo"
      value_template: >-
        {% if is_state('input_boolean.heater_program1', 'on') or is_state('input_boolean.heater_program2', 'on') or is_state('input_boolean.heater_program3', 'on') or is_state('input_boolean.heater_program4', 'on') %}
          on
        {% else %}
          off
        {% endif %}
    rs100_state:
      friendly_name: "Stato Luci Zona Notte"
      value_template: >-
        {% if is_state('binary_sensor.rs101_state', 'on') or is_state('binary_sensor.rs102_state', 'on') or is_state('binary_sensor.rs103_state', 'on') or is_state('binary_sensor.rs104_state', 'on') %}
          on
        {% else %}
          off
        {% endif %}
    rs101_state:
      friendly_name: "Stato Tapparelle Zona 101"
      value_template: >-
        {% if is_state('sensor.rs1011', 'unknown') and is_state('sensor.rs1012', 'unknown') and is_state('sensor.rs01', 'unknown') %}
          off
        {% else %}
          on
        {% endif %}
    rs102_state:
      friendly_name: "Stato Tapparelle Zona 102"
      value_template: >-
        {% if is_state('sensor.rs1021', 'unknown') and is_state('sensor.rs1022', 'unknown') and is_state('sensor.rs02', 'unknown') %}
          off
        {% else %}
          on
        {% endif %}
    rs103_state:
      friendly_name: "Stato Tapparelle Zona 103"
      value_template: >-
        {% if is_state('sensor.rs1031', 'unknown') and is_state('sensor.rs1032', 'unknown') and is_state('sensor.rs03', 'unknown') %}
          off
        {% else %}
          on
        {% endif %}
    rs104_state:
      friendly_name: "Stato Tapparelle Zona 104"
      value_template: >-
        {% if is_state('sensor.rs1041', 'unknown') and is_state('sensor.rs1042', 'unknown') %}
          off
        {% else %}
          on
        {% endif %}
    rs200_state:
      friendly_name: "Stato Luci Zona Giorno"
      value_template: >-
        {% if is_state('binary_sensor.rs201_state', 'on') or is_state('binary_sensor.rs202_state', 'on') or is_state('binary_sensor.rs203_state', 'on') %}
          on
        {% else %}
          off
        {% endif %}
    rs201_state:
      friendly_name: "Stato Tapparelle Zona 201"
      value_template: >-
        {% if is_state('sensor.rs2011', 'unknown') and is_state('sensor.rs2012', 'unknown') and is_state('sensor.rs04', 'unknown') and is_state('sensor.rs05', 'unknown') %}
          off
        {% else %}
          on
        {% endif %}
    rs202_state:
      friendly_name: "Stato Tapparelle Zona 202"
      value_template: >-
        {% if is_state('sensor.rs2021', 'unknown') and is_state('sensor.rs2022', 'unknown') and is_state('sensor.rs06', 'unknown') %}
          off
        {% else %}
          on
        {% endif %}
    rs203_state:
      friendly_name: "Stato Tapparelle Zona 203"
      value_template: >-
        {% if is_state('sensor.rs2031', 'unknown') and is_state('sensor.rs2032', 'unknown') %}
          off
        {% else %}
          on
        {% endif %}
    lz100_state:
      friendly_name: "Stato Luci Zona Notte"
      value_template: >-
        {% if is_state('binary_sensor.lz101_state', 'on') or is_state('binary_sensor.lz102_state', 'on') or is_state('binary_sensor.lz103_state', 'on') or is_state('binary_sensor.lz104_state', 'on') %}
          on
        {% else %}
          off
        {% endif %}
    lcz100_state:
      friendly_name: "Stato Luci Zona Notte"
      value_template: >-
        {% if is_state('sensor.lcz101_state', 'online') or is_state('sensor.lcz102_state', 'online') or is_state('sensor.lcz103_state', 'online') or is_state('sensor.lcz104_state', 'online') %}
          on
        {% else %}
          off
        {% endif %}
    lwz100_state:
      friendly_name: "Stato Luci Zona Notte"
      value_template: >-
        {% if is_state('sensor.lwz101_state', 'online') or is_state('sensor.lwz102_state', 'online') or is_state('sensor.lwz103_state', 'online') or is_state('sensor.lwz104_state', 'online') %}
          on
        {% else %}
          off
        {% endif %}
    lz101_state:
      friendly_name: "Stato Luci Zona 101"
      value_template: >-
        {% if is_state('sensor.lcz101_state', 'online') or is_state('sensor.lwz101_state', 'online') or is_state('sensor.lz101_state', 'online') %}
          on
        {% else %}
          off
        {% endif %}
    lz102_state:
      friendly_name: "Stato Luci Zona 102"
      value_template: >-
        {% if is_state('sensor.lcz102_state', 'online') or is_state('sensor.lwz102_state', 'online') or is_state('sensor.lz102_state', 'online') %}
          on
        {% else %}
          off
        {% endif %}
    lz103_state:
      friendly_name: "Stato Luci Zona 103"
      value_template: >-
        {% if is_state('sensor.lcz103_state', 'online') or is_state('sensor.lwz103_state', 'online') or is_state('sensor.lz103_state', 'online') %}
          on
        {% else %}
          off
        {% endif %}
    lz104_state:
      friendly_name: "Stato Luci Zona 104"
      value_template: >-
        {% if is_state('sensor.lcz104_state', 'online') or is_state('sensor.lwz104_state', 'online') or is_state('sensor.lz104_state', 'online') %}
          on
        {% else %}
          off
        {% endif %}
    lz200_state:
      friendly_name: "Stato Luci Zona Giorno"
      value_template: >-
        {% if is_state('binary_sensor.lz201_state', 'on') or is_state('binary_sensor.lz202_state', 'on') or is_state('binary_sensor.lz203_state', 'on') %}
          on
        {% else %}
          off
        {% endif %}
    lcz200_state:
      friendly_name: "Stato Luci Zona Notte"
      value_template: >-
        {% if is_state('sensor.lcz201_state', 'online') or is_state('sensor.lcz202_state', 'online') or is_state('sensor.lcz203_state', 'online') %}
          on
        {% else %}
          off
        {% endif %}
    lwz200_state:
      friendly_name: "Stato Luci Zona Notte"
      value_template: >-
        {% if is_state('sensor.lwz201_state', 'online') or is_state('sensor.lwz202_state', 'online') or is_state('sensor.lwz203_state', 'online') %}
          on
        {% else %}
          off
        {% endif %}
    lz201_state:
      friendly_name: "Stato Luci Zona 201"
      value_template: >-
        {% if is_state('sensor.lcz201_state', 'online') or is_state('sensor.lwz201_state', 'online') or is_state('sensor.lz201_state', 'online') %}
          on
        {% else %}
          off
        {% endif %}
    lz202_state:
      friendly_name: "Stato Luci Zona 202"
      value_template: >-
        {% if is_state('sensor.lcz202_state', 'online') or is_state('sensor.lwz202_state', 'online') or is_state('sensor.lz202_state', 'online') %}
          on
        {% else %}
          off
        {% endif %}
    lz203_state:
      friendly_name: "Stato Luci Zona 203"
      value_template: >-
        {% if is_state('sensor.lcz203_state', 'online') or is_state('sensor.lwz203_state', 'online') or is_state('sensor.lz203_state', 'online') %}
          on
        {% else %}
          off
        {% endif %}
    lz300_state:
      friendly_name: "Stato Luci Esterno"
      value_template: >-
        {% if is_state('binary_sensor.lz301_state', 'on') or is_state('binary_sensor.lz302_state', 'on') or is_state('binary_sensor.lz303_state', 'on') or is_state('binary_sensor.lz304_state', 'on') %}
          on
        {% else %}
          off
        {% endif %}
    lcz300_state:
      friendly_name: "Stato Luci Zona Notte"
      value_template: >-
        {% if is_state('sensor.lcz301_state', 'online') or is_state('sensor.lcz302_state', 'online') or is_state('sensor.lcz303_state', 'online') or is_state('sensor.lcz304_state', 'online') %}
          on
        {% else %}
          off
        {% endif %}
    lwz300_state:
      friendly_name: "Stato Luci Zona Notte"
      value_template: >-
        {% if is_state('sensor.lwz301_state', 'online') or is_state('sensor.lwz302_state', 'online') or is_state('sensor.lwz303_state', 'online') or is_state('sensor.lwz304_state', 'online') %}
          on
        {% else %}
          off
        {% endif %}
    lz301_state:
      friendly_name: "Stato Luci Zona 301"
      value_template: >-
        {% if is_state('sensor.lcz301_state', 'online') or is_state('sensor.lwz301_state', 'online') or is_state('sensor.lz301_state', 'online') %}
          on
        {% else %}
          off
        {% endif %}
    lz302_state:
      friendly_name: "Stato Luci Zona 302"
      value_template: >-
        {% if is_state('sensor.lcz302_state', 'online') or is_state('sensor.lwz302_state', 'online') or is_state('sensor.lz302_state', 'online') %}
          on
        {% else %}
          off
        {% endif %}
    lz303_state:
      friendly_name: "Stato Luci Zona 303"
      value_template: >-
        {% if is_state('sensor.lcz303_state', 'online') or is_state('sensor.lwz303_state', 'online') or is_state('sensor.lz303_state', 'online') %}
          on
        {% else %}
          off
        {% endif %}
    lz304_state:
      friendly_name: "Stato Luci Zona 304"
      value_template: >-
        {% if is_state('sensor.lcz304_state', 'online') or is_state('sensor.lwz304_state', 'online') or is_state('sensor.lz304_state', 'online') %}
          on
        {% else %}
          off
        {% endif %}
