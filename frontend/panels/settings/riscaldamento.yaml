type: 'custom:button-card'
template: entities_grid
custom_fields:
  header:
    card:
      type: 'custom:button-card'
      label: "Impostazioni / Riscaldamento"
      template:
        - header_page_text
      name: 'IMPOSTAZIONI'
      icon: 'mdi:cog'
  backbutton:
    card:
      type: 'custom:button-card'
      template: button_go_back
  entities:
    card:
      type: vertical-stack
      cards:
        - type: entities
          entities:
            - entity: input_number.manual_heating_temp
              tap_action:
                action: none